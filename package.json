{
  "name": "alliancev2",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "watch": "tailwindcss -i ./src/css/tailwind.css -o ./_site/css/style.css --watch",
    "build": "npx @11ty/eleventy & tailwindcss -i ./src/css/tailwind.css -o ./_site/css/style.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-img": "^3.1.0",
    "tailwindcss": "^3.3.1"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
